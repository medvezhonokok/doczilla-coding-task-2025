### 1. В корне проекта 
```bash
# В корне проекта (где лежит pom.xml)
mvn clean package
```

### 2. Запускаем приложение
```bash
mvn spring-boot:run
```

Сервис будет доступен по ссылке http://localhost:8080